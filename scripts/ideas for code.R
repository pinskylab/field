# write a code that finds every -9999 and creates a text file (individually) to import into QGIS to find the actual anem_id

# create a column called doubt with a 0 1 - if it has a 1 for doubt, look at the notes and see what the cause of the doubt is
